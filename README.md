# Beacon Spear Frontend (Next.js dashboard)

Placeholder repository.

Tech decision:
- Next.js (latest stable) + App Router
- TypeScript
- Tailwind CSS

Planned responsibilities:
- Dashboard UI routes under `/`
- Calls backend JSON APIs under `/api/*` using session cookies + CSRF

See the meta repo design docs for the v0.1 spec.
